meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  command: python main.py
  persistenceMount: /data
  stdout: true
  stderr: true 